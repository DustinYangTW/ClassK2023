@model IEnumerable<MyModel_CodeFirst.Models.Book>

@{
    ViewData["Title"] = "留言板管理";
}
@*//2.1.5 修改Index View將Photo及ImageType欄位、Create、Edit及Details超鏈結移除*@
<h1>@ViewData["Title"]</h1>

@* <p>
    <a asp-action="Create">Create New</a>
</p> *@
<table class="table table-striped table-hover">
    <thead class="bg-dark text-white">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Photo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Description)
            </th>
           @*  <th>
                @Html.DisplayNameFor(model => model.Photo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ImageType)
            </th> *@
            <th>
                @Html.DisplayNameFor(model => model.Author)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TimeStamp)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>

                @if(item.Photo!=null){
                    <img src=" @Url.Action("GetImage",new{gid=item.GId})" width="200">
                }
            </td>

            <td>
                @Html.DisplayFor(modelItem => item.Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
          @*   <td>
                @Html.DisplayFor(modelItem => item.Photo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ImageType)
            </td> *@
            <td>
                @Html.DisplayFor(modelItem => item.Author)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TimeStamp)
            </td>
            <td>
             @*    <a asp-action="Edit" asp-route-id="@item.GId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.GId">Details</a> | *@
                <a asp-action="Delete" asp-route-id="@item.GId" class="btn btn-danger">刪除</a>
            </td>
        </tr>
}
    </tbody>
</table>
